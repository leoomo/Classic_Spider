{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "分析以下代码变更的影响范围：$ARGUMENTS。评估哪些模块、函数可能受影响。简洁回答，只列出受影响范围。"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup|resume|compact",
        "hooks": [
          {
            "type": "command",
            "command": "echo '📋 项目规范提醒: 使用 pnpm (不是 npm/bun)。调试日志: logs/debug.log。启动前检查端口 1420/5173。'"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "检测到可能执行 git push 命令。$ARGUMENTS\n\n如果命令包含 'git push'，请先执行 'bash .claude/scripts/update-version.sh' 更新版本号，然后再执行原命令。如果不是 push 命令，直接回答 'NOOP'。"
          }
        ]
      }
    ]
  }
}
